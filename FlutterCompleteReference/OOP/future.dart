void main() {
  void main() {
        Future<int> val = processData(1, 2.5);
        val.then((result) => print(result));
}

}


class forClass{
  Future<int> newFuture(int x, double, y) {
  var x = 0;
    for(var i = 0; i < x; ++i) {
            for (var j = 0; j < x*y; j++) {
                // a lot of work here...
        }
        }  
 final res = httpGetRequest(x);
        return Future<int>.value(res);
}